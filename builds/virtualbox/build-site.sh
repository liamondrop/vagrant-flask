#!/usr/bin/env bash

echo "$AGENT Running $0"

# if [ ! -d /var/www/$SITE_NAME ]; then
#     echo "$AGENT Make log & site directories for the web"
    
#     # create logs web directories
#     mkdir -p /var/www/$SITE_NAME/logs

#     # symlink web directory to project directory
#     ln -s /home/vagrant/prosper202/ /var/www/$SITE_NAME/htdocs
#     chown -R www-data:www-data /var/www/$SITE_NAME
# fi
